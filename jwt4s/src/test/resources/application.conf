jwt {
  hmac-secret-key-base64 = "secret"
  algorithm = "algorithm"
  audience = "theAudience"
  issuer = "theIssuer"
}

jwt {
  hmac-secret-key-base64 = "secret"
  audience = "theAudience"
  issuer = "theIssuer"
  accepted-alg-headers = ["alg1"]
}